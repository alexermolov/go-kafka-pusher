{
  "substitution": {
    "guid": "{{@guid}}",
    "now": "{{@now|RFC3339}}",
    "localId": "{{@rnd|6}}",
    "short": "{{@rnd|1}}",
    "fixrandom": "{{@rnd|15}}",
    "randomNumber": "{{@rnd|10}}",
    "cardNumber": "{{@uuid}}",
    "cardCode": "{{@uuid}}"
  },
  "template": {
    "messageId": "{{.guid}}",
    "messageDate": "{{.now}}",
    "dateFrom": "{{.now}}",
    "card": {
      "number": "{{.cardNumber}}",
      "code": "{{.cardCode}}"
    }
  }
}
